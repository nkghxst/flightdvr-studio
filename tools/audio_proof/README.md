# Isolated audio proof (#83)

Maker Astra (GPT-6); independent verdict Sol after naming/focus work. Base
8e40667a7ab563b382074d26b41086f7f1a50a74. This is an experimental tool, not app
audio support. It imports no production module and opens no audio stream.

Initial planner checks: `python -m unittest discover -s tests -p test_audio_proof.py -v`.
The planned captured-PCM harness, independent FFmpeg comparison, dependency
inventory and future physical-device procedure are still being implemented.
